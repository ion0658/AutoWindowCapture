<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AutoWindowCapture.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:AutoWindowCapture"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windowEnumerator="using:WindowEnumerator"
        mc:Ignorable="d"
        Title="AutoWindowCapture">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>


    <Grid Padding="16">
        <TabView IsAddTabButtonVisible="False">
            <TabViewItem Header="ウィンドウ一覧"
                         IsClosable="False">
                <TabViewItem.Content>
                    <Grid Padding="0,12,0,0"
                          RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Opacity="0.7"
                                   Text="{x:Bind vm.StatusMessage, Mode=OneWay}" />

                        <ListView Grid.Row="1"
                                  ItemsSource="{x:Bind vm.Windows, Mode=OneWay}"
                                  VerticalAlignment="Stretch"
                                  MinHeight="100">


                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="windowEnumerator:WindowInfo">
                                    <StackPanel Spacing="4"
                                                Orientation="Vertical">
                                        <StackPanel Spacing="4"
                                                    Orientation="Horizontal">
                                            <TextBlock Text="{x:Bind Title}"
                                                       TextWrapping="WrapWholeWords" />
                                        </StackPanel>
                                        <StackPanel Spacing="4"
                                                    Orientation="Horizontal">
                                            <TextBlock Opacity="0.7"
                                                       Text="{x:Bind ProcessName}" />
                                            <TextBlock FontFamily="Consolas"
                                                       Opacity="0.7"
                                                       Text="{x:Bind ProcessId}" />
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>

                        </ListView>
                    </Grid>
                </TabViewItem.Content>
            </TabViewItem>

            <TabViewItem Header="設定"
                         IsClosable="False">
                <Grid Padding="0,12,0,0">
                    <TextBlock Text="{x:Bind vm.SettingsMessage, Mode=OneWay}" />
                </Grid>
            </TabViewItem>
        </TabView>
    </Grid>
</Window>
