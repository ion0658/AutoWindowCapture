<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="WindowListPage.Views.WindowList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:WindowListPage.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:windowEnumerator="using:WindowEnumerator"
      mc:Ignorable="d">
    <ListView Grid.Row="1"
              MinHeight="100"
              ItemsSource="{x:Bind vm.Windows, Mode=OneWay}"
              SelectedItem="{x:Bind vm.SelectedWindow, Mode=TwoWay}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="windowEnumerator:WindowInfo">
                <StackPanel Spacing="4">
                    <TextBlock Text="{x:Bind Title}"
                               TextWrapping="WrapWholeWords" />
                    <TextBlock Opacity="0.7"
                               Text="{x:Bind ProcessName}" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
